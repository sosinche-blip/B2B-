# 어드민플러스 자동화 운영 가이드

## 계정 구성
협력사 셀러 계정마다 별도의 Client ID/Client Secret을 등록합니다. 웹앱의 `협력사명`은 기존 상품매핑의 `업체명`과 정확히 같아야 해당 주문이 올바른 셀러 계정으로 전달됩니다.

## 인증 만료
- AdminPlus Access Token: 공식 문서 기준 30일. Ncloud가 자동 발급/재사용/갱신합니다.
- AdminPlus `401 access token expired`: 공식 문서상 클라이언트 계약 만료 의미도 있으므로, 이 경우 AdminPlus 계약 갱신이 먼저 필요합니다.
- Client ID/Secret을 재발급·교체한 경우 웹앱 설정에서 새 값을 테스트 후 즉시 적용합니다.
- Toss Shopping Access Token: 토큰 응답의 `expires_in`을 기준으로 만료 5분 전 또는 HTTP 401 때 자동 재발급합니다.
- Toss Shopping Access/Secret Key 자체의 고정 만료기간은 공식 인증 문서에 명시되어 있지 않으므로, 파트너스에서 키를 재발급/교체한 경우 웹앱에서 변경합니다.

## 설정시간별 발주등록
1. 쿠팡 `ACCEPT`, 토스 `PAID` 주문을 조회합니다.
2. `채널 + 옵션ID`로 기존 웹앱 매핑을 찾습니다.
3. 매핑의 `업체명`으로 AdminPlus 셀러 계정을 선택합니다.
4. AdminPlus 상품문자열 매칭이 실제 등록되어 있는지 사전 확인합니다.
5. 이미 등록한 `채널 + 주문번호 + 옵션ID`는 이력으로 차단합니다.
6. 자동화 시작 이전 주문은 자동발주에서 제외합니다.
7. `POST /v1/seller/orders`로 주문을 등록합니다.

**결제 접수는 자동화하지 않습니다.** AdminPlus 공식 흐름에서 주문 등록과 결제 접수는 별도 API이고 실제 금전결제가 발생할 수 있기 때문입니다.

## 송장 자동등록
1. AdminPlus `/v1/seller/orders/changed`로 변경분을 조회합니다.
2. `shipping_company + tracking_number`가 생긴 주문만 선택합니다.
3. 발주 이력의 원 주문 식별자와 연결합니다.
4. 쿠팡/토스 상품준비중 처리를 시도합니다.
5. 기존 송장등록 로직으로 쿠팡/토스에 운송장을 등록합니다.
6. 성공한 건만 `shipmentUploadedAt`을 기록해 중복등록을 차단합니다.

## 처음 켤 때
`발주 사전검증` → `송장 사전확인` → 필요하면 `지금 발주 실행` 1~2건 테스트 → 정상 확인 → 자동화 사용 순서를 권장합니다.
