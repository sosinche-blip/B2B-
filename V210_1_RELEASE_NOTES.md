# V210.1 변경사항 — 주문 5단계 운영흐름 수정

## 수정 목적
기존 일일 운영 점검판은 `결제완료 / 상품준비중 / 배송중 / 배송완료` 4단계만 표시하여,
채널에서 결제가 끝난 주문과 자동발주를 위해 실제 수집된 주문을 구분할 수 없었습니다.

## 수정된 운영 흐름
`결제완료 → 수집완료 → 상품준비중 → 배송중 → 배송완료`

- **결제완료**: 쿠팡 `ACCEPT`, 토스 `PAID` 등 채널 API의 결제완료 현재상태
- **수집완료**: 위 결제완료 주문 중 자동발주 처리를 위해 앱에 실제 API 수집된 주문
- **상품준비중**: 쿠팡 `INSTRUCT`, 토스 `PREPARING_PRODUCT`
- **배송중**: 쿠팡 `DEPARTURE/DELIVERING`, 토스 `DELIVERING`
- **배송완료**: 쿠팡 `FINAL_DELIVERY`, 토스 `DELIVERED/CONFIRMED_ORDER`

## UI 변경
- 일일 운영 점검판 상단을 5개 카드로 수정
- `수집완료` 카드와 상세목록 추가
- 마감보고서 `오늘운영요약`에 `수집완료` 수치 추가
- 설명문에 채널 결제상태와 자동발주 수집상태의 차이를 명시

## 집계 원칙
`수집완료`는 자동발주용 API 수집 주문만 집계하며, 수동으로 업로드한 주문 엑셀은 포함하지 않습니다.
수집된 주문이 실제 채널에서 상품준비중으로 넘어가면 `수집완료` 중간단계가 아니라 `상품준비중` 단계에서 보입니다.
