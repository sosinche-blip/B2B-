# V206 쉬운 배포 안내

이번 버전은 웹앱/Cloudflare Worker 소스 패키지입니다. Ncloud는 추가 수정이 없습니다.

## 변경 핵심
- `검증완료·미시작`을 `자동운영 준비완료`로 명확화
- 준비완료 행마다 `자동운영 시작` 버튼 추가
- 상단에 `준비완료 자동운영 시작` 버튼 추가
- 일부 대상이 이미 운영중이어도 준비완료 대상을 추가로 시작 가능
- 기존 안전 쿠폰교체 및 4단계 주문상태 점검 유지

## 배포
1. 현재 GitHub 저장소를 백업합니다.
2. 이 ZIP을 압축 해제합니다.
3. 압축을 푼 폴더 안의 파일/폴더 전체를 기존 GitHub 저장소 main 브랜치에 반영합니다.
4. GitHub Actions / Cloudflare 배포가 성공하는지 확인합니다.
5. 웹앱에서 Ctrl+F5로 강력 새로고침합니다.

## 확인
쿠폰 > 24시간 반복대상 관리에서:
- 상태명이 `자동운영 준비완료`로 표시되는지
- 해당 행에 `자동운영 시작` 버튼이 보이는지
- 준비완료 항목이 있으면 상단에 `준비완료 자동운영 시작` 버튼이 보이는지
- 시작 후 해당 행이 `운영중`으로 바뀌는지 확인합니다.
