# V212 Release Notes

## AdminPlus 매칭
- 자동추천 테이블의 기본수량/배송비를 숫자 입력칸으로 변경.
- 확정된 상품매칭 목록에서도 기본수량/배송비를 직접 수정 후 `수량·배송비 저장` 가능.
- 기본수량 변경 시 AdminPlus product match의 `products[].qty`도 재저장.
- 배송비와 기본수량 변경 시 기존 엑셀 매핑, 서버 설정, 구성원가를 함께 갱신.

## 쿠폰 복구발행
- `지금 쿠폰 교체`에서 저장된 couponId가 없거나 이미 종료된 경우를 정상 복구 시나리오로 처리.
- 대상 옵션의 실제 APPLIED 쿠폰이 0개면 취소 API를 생략하고 신규 생성/적용 단계로 진행.
- 실제 APPLIED 쿠폰이 1개면 해당 쿠폰을 종료 후 신규 발행.
- 같은 옵션의 APPLIED 쿠폰이 2개 이상이면 안전 차단.
- 쿠폰 목록 조회 실패 시 신규 발행 금지.
- APPLIED 쿠폰이 없는 상태에서 신규 발행이 실패해도 반복대상을 삭제/중지하지 않아 수동 재시도 가능.
