# V211 AdminPlus 기본수량·배송비 사용 가이드

## 1. 예시
AdminPlus 기준상품: `활 바지락 1kg (65~80미) 大`
- 상품단가: 3,500원
- 배송비: 4,000원

판매상품 구성:
- 왕바지락 1kg: 기본수량 1 → 3,500 × 1 + 4,000 = 7,500원
- 왕바지락 2kg: 기본수량 2 → 3,500 × 2 + 4,000 = 11,000원
- 왕바지락 5kg: 기본수량 5 → 3,500 × 5 + 4,000 = 21,500원

## 2. 입력 위치
`매핑·발주 → API 상품매칭`에서 매핑대상을 선택한 뒤:
1. AdminPlus 상품/옵션을 확인합니다.
2. `기본수량`을 입력합니다.
3. `배송비(원)`을 입력합니다.
4. 화면의 `구성원가`를 확인합니다.
5. `매칭 저장/확정`을 누릅니다.

기존 엑셀 매핑에는 `기본수량`과 `배송비` 컬럼을 사용할 수 있습니다. 배송비가 없는 기존 파일은 0원으로 처리됩니다.

## 3. 자동발주 수량
`기본수량`은 AdminPlus 상품문자열 매칭의 상품 수량으로 저장됩니다. 쿠팡/토스 주문 등록 시에는 마켓에서 주문된 판매상품 개수만 전달하고, 기본수량은 AdminPlus의 매칭 규칙이 내부에서 확장합니다.

## 4. 가격변동 알림
AdminPlus 상품단가가 바뀌면 다음 두 값을 함께 보여줍니다.
- 단가: 이전 → 현재
- 구성원가: 이전 → 현재

구성원가 계산식은 `상품단가 × 기본수량 + 배송비`입니다.

## 5. 주의
`배송비` 입력값은 웹앱의 원가 기준값이며 AdminPlus의 실제 배송정책을 변경하는 값이 아닙니다. 합배송 최대중량에 따라 배송비를 여러 번 부과하는 별도 규칙은 이번 버전에 자동 적용하지 않습니다.
