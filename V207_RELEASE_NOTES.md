# V207 변경사항

- 쿠폰 중복 판정 기준을 쿠폰명에서 vendorItemId(옵션ID)로 변경했습니다.
- 같은 날짜/같은 쿠폰명이어도 옵션ID가 다르면 정상으로 허용합니다.
- 동일 옵션ID가 서로 다른 APPLIED 쿠폰 2개 이상에 동시에 연결된 경우에만 중복 적용으로 차단합니다.
- 실제 APPLIED 옵션 연결을 기준으로 오래된 couponId를 자동 복구합니다.
- 24시간 반복대상 목록에서 다음 발행 쿠폰명을 직접 수정할 수 있습니다.
- 이름 변경은 현재 발행된 쿠폰에는 영향을 주지 않고 다음 신규 발행 또는 `지금 쿠폰 교체`부터 적용됩니다.
- 쿠폰명/할인조건을 수정하면 사전검증 상태가 `미검증`으로 돌아가므로 다시 검증해야 합니다.
