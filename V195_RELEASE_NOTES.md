# V195 릴리스 노트 — API 경로관리·쿠폰 즉시적용

## 1. API 경로 관리

운영설정에 `API 경로 관리` 표를 추가했습니다.

- 쿠팡 주문조회, 재고조회, 송장등록, 주문확인
- 쿠팡 쿠폰 생성, 상품적용, 취소, 요청상태, 계약서/쿠폰/상품 목록
- 토스 주문조회, 주문상태변경, 송장등록

경로는 `/v2/...` 형식의 상대경로만 저장됩니다. 외부 호스트 주소는 제거하며, 줄바꿈과 잘못된 경로는 적용하지 않습니다. `{vendorId}`, `{couponId}`, `{requestedId}`, `{vendorItemId}` 자리표시자는 실제 값으로 자동 치환됩니다.

화면에서 저장한 경로는 다음에 사용됩니다.

- 웹앱의 수동 API 호출
- Ncloud 고정 IP 게이트웨이 호출
- Supabase에 저장된 운영설정을 읽는 Ncloud 자동 스케줄러

API의 경로/버전 변경은 화면에서 처리할 수 있습니다. 인증 알고리즘, 요청 본문, 쿼리 이름, 응답 구조가 변경되는 경우에는 변환 코드 수정이 필요합니다.

## 2. 쿠폰 24시간 반복 대상 즉시 적용

기존 `다음 발행부터` 버튼 옆에 `즉시 적용`을 추가했습니다.

즉시 적용 절차:

1. 현재 couponId, contractId, 옵션ID, 할인조건 검증
2. 사용자 확인
3. 현재 쿠폰 취소
4. 같은 옵션과 변경된 할인조건으로 신규 쿠폰 생성
5. 신규 쿠폰 상품 적용 및 요청상태 확인
6. 반복 대상의 최신 couponId 갱신 및 서버 저장

기존 쿠폰 취소 후 신규 생성에 실패하면 중복/무할인 위험을 줄이기 위해 해당 반복 대상을 자동 중지하고 실패 상태로 기록합니다.

## 3. 정액/정률 할인

반복 대상별 할인방식을 선택할 수 있습니다.

- 정액(원): 10원 이상, 10원 단위
- 정률(%): 1~99 정수
- 정률 할인은 최대 할인금액 10원 이상 입력

서버는 쿠팡 생성 요청에서 정액은 `PRICE`, 정률은 `RATE`로 변환합니다.

## 4. 버전

- 웹앱: `V195 API 경로관리·쿠폰 즉시적용 운영본`
- Worker/Ncloud API: `v195-api-path-coupon-immediate`
- Ncloud 배포 패키지: V194 runtime API paths
