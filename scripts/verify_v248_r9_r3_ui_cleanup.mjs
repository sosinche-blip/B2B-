import fs from "node:fs";const a=fs.readFileSync("apps/web/src/App.tsx","utf8"),c=fs.readFileSync("apps/web/src/style.css","utf8");function x(v,m){if(!v){console.error("[FAIL] "+m);process.exitCode=1}else console.log("[PASS] "+m)};x(a.includes("<h1>B2B 자동화 시스템</h1>"),"header");x(a.includes("전체계정 선택"),"all accounts");x(c.includes("adminplus-catalog-load-button")&&c.includes("white-space:nowrap"),"button one line");if(process.exitCode)process.exit(process.exitCode);console.log("[PASS] V248 R9.3 UI cleanup verification completed.");