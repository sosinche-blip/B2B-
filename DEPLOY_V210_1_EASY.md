# 웹앱 V210.1 주문 5단계 UI 수정 배포

## 수정 범위
이번 수정은 B2B 웹앱의 `일일 운영 점검판` UI/집계 수정입니다.
Ncloud 고정IP 게이트웨이는 수정하지 않았으므로 Ncloud V206/V213 런타임 재배포는 필요하지 않습니다.

## 정상 주문 흐름
`결제완료 → 수집완료 → 상품준비중 → 배송중 → 배송완료`

- 결제완료: 쿠팡/토스 채널의 결제완료 현재상태
- 수집완료: 결제완료 주문 중 자동발주 처리를 위해 실제 API 수집된 현재 주문
- 상품준비중/배송중/배송완료: 쿠팡/토스 채널 API 현재상태

## 배포
1. 현재 GitHub 저장소를 ZIP으로 백업합니다.
2. 이 ZIP을 풉니다.
3. 바깥 폴더가 아니라 안의 파일/폴더 전체를 기존 GitHub 저장소 `main`에 덮어씁니다.
4. GitHub Actions 배포가 성공인지 확인합니다.
5. Cloudflare Pages Production 배포가 Success인지 확인합니다.
6. `https://b2b-bpt.pages.dev/`에서 `Ctrl+F5`로 강력 새로고침합니다.
7. `오늘운영 → 일일 운영 점검판`에서 다음 5개 카드가 순서대로 보이는지 확인합니다.
   - 결제완료
   - 수집완료
   - 상품준비중
   - 배송중
   - 배송완료
8. `결제완료` 카드의 작은 설명이 `채널 결제`, `수집완료` 카드가 `자동발주 수집`으로 표시되는지 확인합니다.
9. 결제완료 주문을 선택수집한 뒤 `수집완료` 숫자/목록이 반영되는지 확인합니다.
10. 상품준비중 처리 후 해당 주문이 수집완료 중간단계가 아니라 상품준비중에서 보이는지 확인합니다.

## 검증 명령
의존성이 설치된 개발환경에서는 아래를 실행할 수 있습니다.

```bash
npm run verify:v210.1
npm run verify:v210
npm run verify:v209
npm run build
```
