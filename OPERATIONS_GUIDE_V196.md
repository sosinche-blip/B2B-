# V196 운영 사용법

## 매일 사용하는 화면

`오늘운영`에서 주문조회, 선택 주문 수집, 업체송장 선택, 쿠팡·토스 업로드를 처리합니다. 상세 조회조건이나 과거 기능이 필요할 때만 `상세 주문 열기`를 누릅니다.

## 매핑과 발주

`매핑·발주`에서 상품 매핑, 엑셀 양식, 발주 파일 하위 탭을 사용합니다. 서버 설정은 이 화면에 중복 표시하지 않습니다.

## 쿠폰

목록 새로고침 → 대상 선택 → 반복대상 추가 → 사전검증 → 자동운영 시작 순서로 사용합니다. 할인조건을 바꾼 뒤:

- 다음 발행부터: 현재 쿠폰 유지
- 즉시 적용: 현재 쿠폰 취소 후 새 조건으로 생성·적용

즉시 적용은 실제 운영 쿠폰을 바꾸므로 couponId와 상품을 확인한 뒤 실행합니다.

## 설정

일반적으로 쿠팡 Secret Key 교체 카드만 사용합니다. 서버 저장, API 경로, 안전 Gate는 장애 대응이나 공식 API 변경 시에만 펼칩니다.
