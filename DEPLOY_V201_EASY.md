# 웹앱 V201 쉬운 배포 안내

1. 현재 GitHub 저장소를 ZIP으로 백업합니다.
2. `B2B_OPERATION_MASTER_V201_OPERATION_BOARD_PURCHASE_COUPON.zip`을 압축 해제합니다.
3. 바깥 폴더가 아니라 압축을 푼 폴더 안의 파일과 폴더 전체를 GitHub `main` 브랜치에 업로드합니다.
4. GitHub Actions에서 배포 작업이 초록색 성공인지 확인합니다.
5. Cloudflare Workers & Pages에서 최신 배포가 Success인지 확인합니다.
6. 웹앱에서 `Ctrl+F5`를 눌러 새로고침합니다.
7. 화면 상단 버전이 `V201 운영현황·발주·쿠폰구조 개선본`인지 확인합니다.

## 확인 순서
- 오늘운영: 8개 현황 박스를 각각 눌러 목록이 열리는지 확인
- 매핑·발주 > 발주관리: 안내 순서와 새 버튼명이 보이는지 확인
- 폴더 선택 후 취소: 오류 대신 `폴더 선택을 취소했습니다`가 표시되는지 확인
- 쿠폰: `24시간 관리에 아직 없는 기존 쿠폰` 목록에 이미 관리 중인 옵션ID가 제외되는지 확인

이번 버전은 Ncloud를 다시 배포하지 않습니다.
