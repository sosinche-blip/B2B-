# AdminPlus 상품매칭·업체구분·공급가 알림 운영 가이드

## 1. 쿠팡·토스 공통 사용
기존 상품매핑의 `채널 + 옵션ID`를 기준으로 쿠팡과 토스 주문 모두 지원합니다. AdminPlus 계정의 `협력사명`은 기존 상품매핑의 `업체명`과 정확히 같아야 합니다.

## 2. 업체 발주방식 자동 구분
`매핑·발주 → 상품 매핑`의 `발주방식` 열과 `API 상품매칭`의 업체표에서 확인합니다.

- `수동/엑셀`: AdminPlus 계정이 연결되지 않은 업체. 기존 업체 엑셀 발주를 그대로 사용합니다.
- `API 연결·중지`: API 계정은 있으나 전체 자동화 또는 해당 업체 자동발주가 OFF입니다. 수동발주가 가능합니다.
- `API 확인필요`: 토큰 또는 `order.read/product.read` 권한에 문제가 있습니다. 자동발주 전에 조치가 필요합니다.
- `AdminPlus API`: 정상 연결되고 자동발주가 활성화된 업체입니다. 이 업체의 주문은 수동 발주파일에서 제외됩니다.

## 3. AdminPlus 상품 직접 매칭
1. `설정 → 어드민플러스 셀러 API 다계정 관리`에서 계정을 등록하고 연결·만료 확인을 합니다.
2. `매핑·발주 → API 상품매칭`으로 이동합니다.
3. AdminPlus 계정을 선택합니다.
4. 같은 업체의 기존 쿠팡/토스 상품·옵션 매핑을 선택합니다.
5. `상품목록 불러오기`를 누릅니다.
6. AdminPlus 상품과 옵션을 선택하고 발주수량을 확인합니다.
7. `매칭 저장·검증`을 누릅니다.
8. 저장 후 다시 조회하여 실제 매칭이 확인돼야 성공 처리됩니다.

기존 상품문자열이 1:N 또는 수량>1로 구성되어 있으면 웹앱이 단일 상품으로 덮어쓰지 않고 중단합니다. 이 경우 AdminPlus의 기존 매칭 구성을 먼저 확인하십시오.

## 4. 공급가 변동 알림
상품 직접 매칭이 완료되는 순간의 AdminPlus 상품가격을 `기준가`로 저장합니다.

- 자동확인 기본시간: `08:30, 13:30, 18:30` KST
- 위치: `자동화 → 어드민플러스 설정시간별 발주·운송장 자동화`
- 즉시확인: `매핑·발주 → API 상품매칭 → 지금 가격확인`
- 가격이 달라지면 웹앱 상단에 `어드민플러스 공급가 변동 N건` 경고가 표시됩니다.
- 기존가/변경가/차액/변동률을 확인할 수 있습니다.
- 판매가와 마진을 검토한 뒤 `현재가를 기준으로`를 누르면 새 가격을 기준가로 승인합니다.

AdminPlus Seller API 문서상 옵션에는 별도의 가격 필드가 없으므로 상품 단위 `price`를 감시합니다.

## 5. 중복발주 방지
AdminPlus 자동발주가 활성화된 업체의 주문은 기존 수동/엑셀 발주파일 생성 대상에서 자동 제외됩니다. 따라서 같은 주문이 API 발주와 엑셀 발주에 동시에 들어가지 않습니다.

자동화를 끈 업체는 다시 수동/엑셀 발주 대상에 포함됩니다.

## 6. 권장 최초 운영 순서
1. AdminPlus 계정 1개 연결 확인
2. 상품 1~2개 직접 매칭
3. `발주 사전검증`
4. 가격 `지금 가격확인`
5. 소량 `지금 발주 실행`
6. 송장 `사전확인` 후 `지금 송장 회수·등록`
7. 이상 없으면 나머지 업체/상품을 등록하고 자동화를 사용합니다.
