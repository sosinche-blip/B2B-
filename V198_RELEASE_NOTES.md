# V198 변경사항

## 1. 매핑 전용 자동 동기화

- 매핑 추가·수정·삭제 후 1초 뒤 Supabase에 자동 저장합니다.
- 전체 운영설정을 덮어쓰지 않고 `operation_persistent_settings`의 `mappings` 항목만 병합합니다.
- PC와 모바일이 앱을 열 때 서버 최신 매핑을 자동으로 불러와 병합합니다.
- 선택 주문 수집 직전에도 서버 최신 매핑을 다시 확인합니다.
- 서버 저장 실패 시 브라우저 자료는 유지하고 화면에 실패 상태를 표시합니다.

## 2. 앱 직접 신규등록

- 엑셀 없이 채널, 옵션ID/옵션관리코드, 업체명, 코드번호, 업체상품명, 원가, 기본수량을 입력해 등록할 수 있습니다.
- 미매핑 주문 목록에서 상품을 선택하면 채널과 옵션ID를 자동으로 채웁니다.
- 같은 채널+옵션ID가 이미 있으면 중복 생성하지 않고 기존 매핑을 수정합니다.

## 3. 엑셀 업로드 안전화

- 엑셀 업로드가 기존 매핑 전체를 교체하지 않고 서버·브라우저 매핑과 병합됩니다.
- 업로드된 행에는 수정시각을 기록해 최신 변경을 우선 적용합니다.

## 4. 기존 기능 유지

- 쿠팡+토스 통합 주문조회
- 모바일 선택 주문 수집
- 쿠팡 Secret Key 교체
- 정액·정률 쿠폰과 즉시 적용
- 주소 상세정보 보존

## 배포 범위

이번 버전은 웹앱과 Cloudflare Worker만 배포합니다. Ncloud 고정 IP 중계 서버는 수정하지 않습니다.
