# V224 AdminPlus 결제정책 수정

- 결제수단은 AdminPlus 예치금(deposit)입니다.
- 업체별 자동결제 ON, 1회 한도 > 0, 일일 한도 > 0을 명시적으로 설정/저장합니다.
- 결제설정이 빠진 발주대상 업체가 있으면 `지금 발주·결제 실행`은 주문등록 전에 중단됩니다.
- 사전검증 응답에 `paymentBlockers`와 `paymentReady`를 노출합니다.
- 주문등록 후에는 기존대로 잔액 확인 → 결제 POST → 결제상태 재조회 → 상품준비중 전환 순서입니다.
