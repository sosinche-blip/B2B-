# V206 변경사항

## 자동운영 시작 UI 보강
기존에는 전체 자동운영 설정이 이미 활성화되어 있으면 상단 버튼이 `자동운영 중지`로 바뀌어, 별도로 `검증완료·미시작`인 반복대상을 시작할 방법이 사라질 수 있었습니다.

V206에서는 전체 설정과 개별 반복대상 활성화를 분리했습니다.

- `검증완료·미시작` → `자동운영 준비완료`
- 준비완료 행마다 `자동운영 시작` 버튼 제공
- 상단에 `준비완료 자동운영 시작` 버튼 제공
- 기존 운영중 항목은 유지한 채 준비완료 항목만 추가 활성화
- 시작 시 서버에 즉시 저장되고 해당 행의 상태를 `운영중`으로 전환

기존 V205의 안전 쿠폰교체와 4단계 주문상태 점검 기능은 유지합니다.
