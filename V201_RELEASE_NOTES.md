# V201 변경사항

## 일일 운영 점검판
- 8개 현황 박스를 버튼으로 변경했습니다.
- 결제완료, 현재 수집주문, 오늘 발주완료, 미매핑, 주소 차단·주의, 송장등록 준비, 미해결 실패, 상품준비중을 클릭하면 해당 목록을 바로 표시합니다.

## 발주관리
- 발주 작업을 `발주 전 검사 → 발주파일 만들기 → 발주완료 기록·중복 차단` 순서로 재배치했습니다.
- 발주 폴더는 선택 기능이며, 설정하지 않아도 브라우저 다운로드로 사용할 수 있음을 명확히 표시했습니다.
- 폴더 선택창을 취소하면 AbortError 대신 정상 취소 안내를 표시합니다.
- 버튼명을 실제 수행 작업이 드러나도록 변경했습니다.

## 쿠폰관리
- 기존 쿠폰 목록에는 24시간 반복대상과 옵션ID가 겹치지 않는 쿠폰만 표시합니다.
- 이미 관리 중인 source/latest couponId도 후보에서 제외합니다.
- 후보 조회 시 각 쿠폰의 적용 옵션ID를 함께 조회합니다.
- 후보를 반복대상으로 추가하면 기존 쿠폰의 원래 기간과 무관하게 24시간 반복관리 초안으로 저장합니다.
- 사전검증 후 자동운영을 시작하면 매일 종료·재발행 스케줄로 운영됩니다.

## 배포 범위
- 웹앱 및 Cloudflare Worker만 배포합니다.
- Ncloud V197은 그대로 유지합니다.
