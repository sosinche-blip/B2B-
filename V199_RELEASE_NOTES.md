# V199 변경사항

## 매핑 자동동기화 404 수정
- `/api/operation/mappings/load`, `/upsert`가 배포되지 않은 기존 Worker에서는 `/api/operation/settings/load`, `/save`로 자동 전환합니다.
- 기존 Worker와 신규 Worker 모두에서 PC·모바일 매핑 동기화를 사용할 수 있습니다.
- 호환모드 저장 시 서버 전체 설정을 먼저 읽고 매핑만 병합한 뒤 다시 저장해 다른 설정을 보존합니다.

## 매핑 운영을 엑셀 방식으로 환원
- 앱 직접 신규등록 폼 제거
- 자동 미매핑 초안 생성 제거
- `미매핑 엑셀` 다운로드 → 업체정보 입력 → `매핑 엑셀 업로드·병합` 흐름으로 통일
- 기존 매핑 표의 확인·수정·삭제와 서버 자동동기화는 유지

## 배포 범위
- 웹앱/Cloudflare Worker 저장소만 배포
- Ncloud 고정 IP 게이트웨이는 수정하지 않음
