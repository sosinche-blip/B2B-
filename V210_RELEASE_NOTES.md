# V210 릴리스 노트 — 기존 엑셀매핑 자동추천 + 검색형 AdminPlus 매칭

## 목적
V209의 직접 상품매칭 화면이 초보 사용자에게 어려웠던 문제를 줄이기 위해, 기존 엑셀 매핑을 먼저 활용해 AdminPlus 매칭 후보를 자동으로 불러오고 사용자가 확인 후 확정하도록 개선했습니다.

## 주요 변경
- 선택한 AdminPlus 계정의 `업체명`과 기존 엑셀 매핑의 `업체명`을 먼저 연결합니다.
- 기존 엑셀의 `업체상품명`을 AdminPlus `match_string`과 정확 비교해 기존 매칭을 자동추천합니다.
- 같은 업체 + 같은 업체상품명으로 이미 확정된 쿠팡/토스 매칭은 다른 채널에서도 재사용 후보로 표시합니다.
- 업체상품코드가 AdminPlus 상품코드와 정확히 일치하거나, 업체상품명이 AdminPlus 상품명과 정확히 일치하는 단일상품은 보조 후보로 표시합니다.
- 자동추천은 저장하지 않습니다. 반드시 사용자가 `매칭 확정`을 눌러야 서버에 반영됩니다.
- AdminPlus 1:N 다상품 매칭은 자동확정하지 않고 `복합매칭확인`으로 남깁니다.
- 기존 단일상품 매칭에서 수량이 2개 이상인 경우는 기존 수량을 유지한 채 확인·확정할 수 있습니다.
- 엑셀매핑 검색: 업체명, 업체상품명, 옵션ID, 코드
- AdminPlus 상품 검색: 상품명, 상품코드, 옵션명, 옵션코드
- 자동추천 결과 검색 추가
- AdminPlus 상품문자열 매칭 목록을 cursor/has_more로 여러 페이지 조회하도록 Worker 보강

## 연결정보 해석
V210에서 자동추천에 사용하는 기존 연결정보는 다음입니다.
1. 업체명
2. 업체상품명(= AdminPlus 주문에 사용하는 상품문자열 후보)
3. 업체상품코드
4. 기존 B2B 바로가기 업체/URL 정보
5. 이미 확정된 AdminPlus 상품매칭

## 서버
Ncloud V203은 변경하지 않습니다. V210은 웹앱 + Cloudflare Worker 업데이트만 필요합니다.
