# V213 운영 가이드 — 서버확정 매핑 / 옵션별 최대 2회 발주 / 자동감시 B알림

## 1. API 상품매칭의 확정 규칙
API 상품매칭 화면에 들어가면 서버의 확정 매핑을 먼저 불러옵니다. 화면의 입력값을 바꾸는 것만으로 실제 자동발주 설정은 바뀌지 않습니다.

수정 흐름:
1. 발주시간 / 기본수량 / 배송비를 편집
2. 행이 `수정 확정` 가능한 상태로 변경
3. `수정 확정` 클릭
4. AdminPlus 실제 매칭 재적용 및 상품코드·옵션코드·수량 재조회 검증
5. 운영설정 서버 저장
6. 서버를 다시 읽어 발주시간·기본수량·배송비·상품/옵션코드가 같은지 검증
7. 모두 일치할 때만 `확정 완료`

저장 또는 재조회가 실패하면 `확정 완료`로 바꾸지 않으며, 직전 서버 확정값이 실제 자동발주 값으로 계속 사용됩니다.

## 2. 옵션별 발주시간
허용 형식은 다음 두 가지뿐입니다.
- `09:00`
- `09:00,14:00`

최대 2개입니다. 중복 시간 또는 3개 이상은 저장할 수 없습니다.

두 시간이 설정돼도 같은 주문을 두 번 발주하지 않습니다. 첫 시간에 이미 발주된 주문은 발주이력 키로 차단되고, 두 번째 시간은 첫 시간 이후 유입됐거나 첫 시간에 아직 처리되지 않은 주문을 처리하는 두 번째 슬롯입니다.

## 3. 자동감시 버튼
### 감시기준 저장
해당 행의 발주시간·기본수량·배송비를 서버에 저장하고 즉시 서버 재조회 검증합니다. 입력 중인 값은 저장 전까지 임시 편집값입니다.

### 현재가를 새 기준가로 적용
가격 변동을 확인한 후 현재 AdminPlus 단가를 정상 가격으로 승인할 때 사용합니다. 미저장 수량/배송비/시간 편집이 있는 동안에는 누르지 못하도록 차단합니다.

### 자동감시 설정 전체 서버저장
현재 확정된 자동감시 설정을 서버에 저장합니다. 행 단위에서 아직 `감시기준 저장`하지 않은 임시 편집값은 전체 저장에 포함하지 않습니다.

## 4. B안 알림
### 웹 화면
- 저장 중 / 저장 성공 / 저장 실패 상태 표시
- 저장 실패 시 빨간 경고
- 가격 변동 발생 시 기존 공급가 변동 알림 표시

### 일일 운영 점검판
- `자동감시 저장 실패`: 미해결 저장 실패 건수
- `가격 변동 감지`: 미확인 AdminPlus 가격변동 건수

두 지표가 0이 아니면 확인이 필요한 운영상태로 표시됩니다.

## 5. 일일 주문상태 흐름
`결제완료 → 수집완료 → 상품준비중 → 배송중 → 배송완료`

`수집완료`는 **AdminPlus 주문등록(발주)이 성공했고 AdminPlus 결제는 아직 완료되지 않은 주문만** 의미합니다.
