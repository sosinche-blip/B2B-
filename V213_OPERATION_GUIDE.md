# V213 자동발주·결제 운영 가이드

## 일일 운영 점검판
- **결제완료**: 쿠팡 `ACCEPT` / 토스 `PAID`인 마켓 결제완료 주문. AdminPlus 발주 전 기본 단계입니다.
- **수집완료**: 위 결제완료 주문 중 **AdminPlus 주문등록(발주)이 성공했고 AdminPlus 결제는 아직 완료되지 않은 주문만** 표시합니다. 발주 이력이 없는 주문은 수집완료로 잡지 않습니다.
- **상품준비중**: AdminPlus 결제완료 확인 후 마켓이 쿠팡 `INSTRUCT` / 토스 `PREPARING_PRODUCT`로 전환된 주문입니다.
- 이후 배송중·배송완료는 기존과 동일합니다.

운영 흐름은 `결제완료 → 수집완료 → 상품준비중 → 배송중 → 배송완료`입니다.

## 옵션별 발주시간
`매핑·발주 → API 상품매칭`에서 각 옵션별로 발주시간을 입력합니다. 같은 시간의 옵션은 한 번의 스케줄 실행에서 함께 처리합니다. `자동화` 화면에는 전역 발주시간이 없습니다.

## 토스 매핑
토스는 주문의 `stockId`와 `productItemManagementCode`가 서로 다를 수 있으므로 두 값과 옵션관리코드 후보를 모두 기존 매핑과 비교합니다.

## 예치금 자동결제
업체별로 다음 네 조건이 모두 맞아야 실행됩니다.
1. 자동발주 ON
2. 예치금 자동결제 ON
3. 1회 결제한도 > 0
4. 일일 결제한도 > 0

추가로 `payment.read`, `balance.read` 권한과 충분한 예치금 잔액이 필요합니다. 결제 완료를 확인하기 전에는 쿠팡/토스를 상품준비중으로 변경하지 않습니다.

**강제 현금영수증 가맹점**은 예치금 전액 결제에도 별도의 현금영수증 정보가 필요할 수 있습니다. 이 경우 자동결제가 실패 상태로 남고 마켓 상태는 바뀌지 않으므로 먼저 소액 테스트가 필요합니다.

## Ncloud 관리 토큰
토큰은 보안을 위해 영구 저장하지 않습니다. V213은 `sessionStorage`에만 보관하므로 **같은 탭에서 새로고침할 때는 다시 입력하지 않아도 되지만**, 새 탭·새 브라우저 세션에서는 다시 입력해야 합니다.
