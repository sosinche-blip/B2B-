# V204 변경사항

## 1. 쿠폰 즉시교체 안전화
- 옵션ID 기준으로 실제 APPLIED 기존 쿠폰을 다시 찾습니다.
- 저장된 couponId가 상품 0개 신규쿠폰을 가리켜도 실제 옵션이 붙은 쿠폰을 우선합니다.
- 같은 옵션이 2개 이상의 APPLIED 쿠폰에 동시에 붙어 있으면 자동교체를 중단합니다.
- 기존 쿠폰 파기 requestedId가 DONE이어도 APPLIED 목록에서 실제 제거될 때까지 신규 쿠폰을 생성하지 않습니다.
- 새 쿠폰은 대상 vendorItemId가 실제 APPLIED 상품목록에 확인된 경우에만 현재 couponId로 저장합니다.
- 상품 0개 등 불완전 신규쿠폰은 현재쿠폰으로 채택하지 않고 자동정리를 요청합니다.
- 반복대상 행의 버튼명은 `지금 쿠폰 교체`로 명확하게 변경했습니다.

## 2. 일일 운영 점검판
상단 운영지표는 다음 5개만 표시합니다.
- 결제완료
- 상품준비중
- 배송중
- 배송완료
- 배송확인

숫자와 클릭 목록은 모두 같은 실시간 API 조회 결과를 사용합니다.

쿠팡 상태 분류:
- ACCEPT -> 결제완료
- INSTRUCT -> 상품준비중
- DEPARTURE / DELIVERING -> 배송중
- FINAL_DELIVERY -> 배송완료

토스 상태 분류:
- PAID -> 결제완료
- PREPARING_PRODUCT -> 상품준비중
- DELIVERING -> 배송중
- DELIVERED / CONFIRMED_ORDER -> 배송완료

`배송확인`은 배송중 상태의 마지막 상태시각 기준 다음날 오전 06:00이 지났는데도 현재 배송중인 주문입니다. 상태변경 시각을 API에서 받을 수 없는 행은 주문시각을 보수적인 대체 기준으로 사용합니다.

## 3. 제거한 운영 UI
- 현재 수집주문 지표
- 송장등록 준비 지표
- 미해결 실패 지표
- 세부 운영점검 8개 보기
- 실패 재처리 영역

주소 품질검사는 하단 보조영역으로 유지합니다.
