# V196 변경사항

## 메뉴·화면 정리

- 상단 메뉴를 8개에서 5개로 축소했습니다.
- 주문관리는 오늘운영의 `상세 주문 열기`로 통합했습니다.
- 매핑관리·양식설정·발주관리는 `매핑·발주` 작업공간의 하위 탭으로 통합했습니다.
- 간편운영에 중복 표시되던 서버 저장·설정 패널을 제거했습니다.
- 일일 운영의 주소검사·실패 재처리·세부점검은 필요할 때만 펼치도록 변경했습니다.
- API 경로와 안전 Gate는 설정의 고급 영역으로 이동했습니다.

## 쿠팡 인증키 교체

- 새 Secret Key 입력 및 확인
- Access Key·Vendor ID가 함께 바뀐 경우에만 고급 입력
- 새 키 연결 테스트
- 성공할 때만 Ncloud `.dev.vars` 백업 및 즉시 적용
- 적용 성공 후 브라우저 메모리의 Secret Key·관리 토큰 초기화
- 인증키 요청은 HTTPS Cloudflare Worker만 사용하며 직접 HTTP 게이트웨이로 우회하지 않음
- Cloudflare→Ncloud 구간은 AES-256-GCM 보안 봉투로 암호화

## 쿠폰 화면 정리

기본 버튼은 다음 네 가지로 정리했습니다.

1. 쿠폰 목록 새로고침
2. 선택 쿠폰 반복대상 추가
3. 사전검증
4. 자동운영 시작 또는 중지

실제 쿠폰 취소와 현황 다운로드만 고급·위험 작업에 남겼습니다. 요청상태 확인, 선택 해제, 반복설정 전체삭제 버튼은 기본 화면에서 제거했습니다. 반복대상은 각 행에서 개별 삭제합니다.

24시간 반복대상 표는 기술 ID를 쿠폰명 아래 보조정보로 합쳐 열 수를 줄였습니다. 정액·정률 할인, 정률 최대 할인금액, 다음 발행부터, 즉시 적용 기능은 유지합니다.
