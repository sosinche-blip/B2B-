# V200 변경사항

## 새 쿠폰 실행 흐름

- API 옵션ID 조회, 사전검증, 즉시 적용, 다음 발행부터를 같은 영역에 배치했습니다.
- 사전검증 결과 문구에서 아직 쿠폰이 발행되지 않았음을 명확히 표시합니다.
- `즉시 적용`은 실제 쿠폰 생성·상품 적용·24시간 반복대상 등록을 수행합니다.
- `다음 발행부터`는 현재 쿠폰을 발행하지 않고 다음 정기 발행 대상으로 저장합니다.

## 기존 쿠폰과 반복대상 UI

- 쿠폰 목록 바로 아래에 반복대상 추가·실제 취소·다운로드 버튼을 배치했습니다.
- 반복대상 목록 바로 위에 전체 사전검증·자동운영 시작/중지 버튼을 배치했습니다.
- 기능을 작업 단계 1·2·3으로 구분했습니다.

## 쿠폰 취소 처리

- 파기 API 요청을 couponId당 한 번만 보냅니다.
- 반환된 requestedId를 즉시·10초·30초에 확인합니다.
- 30초 안에 DONE이 아니면 파기 요청을 반복하지 않고 requestedId 상태만 주기적으로 확인합니다.
- 종료 완료 전 신규 쿠폰 발행을 보류해 중복 쿠폰 적용을 방지합니다.
