# V212 운영 가이드

## AdminPlus 기본수량·배송비 수동 수정
- `매핑·발주 → API 상품매칭`
- 자동추천 결과의 `기본수량`, `배송비`는 직접 수정할 수 있습니다.
- 수정 후 `매칭 확정`을 누르면 AdminPlus 매칭 수량, 기존 엑셀매핑, 서버 저장값이 함께 갱신됩니다.
- 이미 확정된 매칭도 하단 공급가 감시 표에서 값을 수정하고 `수량·배송비 저장`을 누를 수 있습니다.

## 종료된 반복쿠폰 복구
`지금 쿠폰 교체`는 대상 옵션의 현재 APPLIED 상태를 기준으로 동작합니다.
- 1개: 실제 APPLIED 쿠폰 종료 → 종료 확인 → 신규 생성 → 옵션 적용 확인.
- 0개: 이미 종료된 것으로 판단 → 취소 생략 → 신규 생성 → 옵션 적용 확인.
- 2개 이상: 중복쿠폰 안전차단.
- 조회 실패: 신규 발행 금지.

신규 발행 성공 후에는 `전체 사전검증`을 다시 실행합니다.
