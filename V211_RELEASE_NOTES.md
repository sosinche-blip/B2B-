# V211 릴리스 노트 — AdminPlus 기본수량·배송비·구성원가

## 핵심 변경
- 기존 엑셀/웹 매핑에 `배송비` 필드를 추가했습니다. 기존 자료는 값이 없으면 0원으로 호환됩니다.
- AdminPlus API 상품매칭 화면의 `발주수량`을 실제 의미에 맞게 `기본수량`으로 변경했습니다.
- 상품매칭 확정 시 `기본수량`과 `배송비`를 함께 저장하고 기존 매핑에도 동기화합니다.
- 구성원가는 `AdminPlus 상품단가 × 기본수량 + 배송비`로 계산합니다.
- 자동추천 목록과 가격변동 알림에서 단가뿐 아니라 구성원가도 함께 표시합니다.

## 수량 중복계산 방지 중요 수정
AdminPlus 상품문자열 매칭의 `products[].qty`에 기본수량이 이미 저장되므로, 주문 등록 `items[].qty`에는 쿠팡/토스에서 주문된 판매상품 수량만 전달합니다.

예: 왕바지락 2kg 상품이 `활 바지락 1kg × 기본수량 2`로 매칭되어 있고 마켓에서 왕바지락 2kg를 3개 주문하면,
- AdminPlus 매칭 기본수량: 2
- AdminPlus 주문 items[].qty: 3
- 실제 기초상품 확장량: 2 × 3 = 6

기본수량을 주문 qty에 다시 곱해 6을 보내는 중복계산을 차단했습니다.

## 배송비의 의미
V211의 `배송비`는 판매구성 원가와 가격변동 검토에 사용하는 셀러 기준값입니다. AdminPlus 자체 배송정책을 수정하지 않습니다.
