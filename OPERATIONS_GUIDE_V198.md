# V198 운영 사용법

## 앱에서 신규 매핑 등록

1. `매핑·발주 → 상품 매핑`으로 이동합니다.
2. 주문에서 발견된 신규상품이면 `미매핑 주문에서 가져오기`를 선택합니다.
3. 업체명, 코드번호, 업체상품명, 원가, 기본수량을 입력합니다.
4. `신규등록`을 누릅니다.
5. 상단 상태가 `서버 자동 저장 완료`로 바뀌는지 확인합니다.

옵션ID/옵션관리코드, 업체명, 업체상품명은 필수입니다. 같은 채널과 옵션ID가 이미 있으면 기존 행을 갱신합니다.

## PC와 모바일 동기화

- 앱 시작 시 서버 최신 매핑을 자동 병합합니다.
- 선택 주문 수집 직전에도 서버 최신 매핑을 다시 불러옵니다.
- 즉시 확인하려면 `서버 최신 매핑` 버튼을 누릅니다.
- 서버 연결이 실패해도 현재 기기의 브라우저 자료는 삭제되지 않습니다.

## 엑셀 업로드

`엑셀 병합`은 기존 매핑을 삭제하지 않고 업로드 파일의 행을 추가하거나 같은 채널+옵션ID의 행을 갱신합니다.

## 삭제

매핑 행의 `삭제`를 누르면 해당 채널+옵션ID가 서버에서도 자동 삭제됩니다. 서버 저장 상태가 완료될 때까지 페이지를 닫지 않는 것이 안전합니다.
