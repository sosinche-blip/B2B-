# V209 릴리스 노트

버전: **V209 어드민플러스 상품직접매칭·업체발주구분·가격변동알림**

## 핵심 변경
- 쿠팡 `ACCEPT`, 토스쇼핑 `PAID` 주문을 동일한 AdminPlus 자동발주 구조에서 지원합니다.
- 기존 상품매핑의 업체를 자동 분류합니다.
  - AdminPlus 계정 미연결: `수동/엑셀`
  - 계정 연결됐으나 자동화 OFF: `API 연결·중지`
  - 인증/권한 이상: `API 확인필요`
  - 정상 연결 + 자동발주 사용: `AdminPlus API`
- AdminPlus API 자동발주 업체 주문은 기존 수동/엑셀 발주파일에서 자동 제외하여 이중 발주를 방지합니다. 자동화가 OFF인 경우에는 기존 수동 발주 흐름을 유지합니다.
- `매핑·발주 → API 상품매칭` 화면을 추가했습니다.
  - AdminPlus 계정 선택
  - 기존 쿠팡/토스 옵션ID 매핑 선택
  - AdminPlus 상품목록 조회
  - 상품/옵션 선택
  - 상품문자열 매칭 저장 후 즉시 재조회 검증
- 기존 AdminPlus 매칭이 1:N 또는 수량>1인 경우 1개 상품 UI로 덮어쓰지 않도록 HTTP 409로 차단합니다.
- 상품 매칭 시 당시 AdminPlus 상품 `price`를 기준가격으로 저장합니다.
- 설정시간별 공급가 자동확인과 즉시 가격확인을 추가했습니다. 기본 권장 시간은 `08:30, 13:30, 18:30`이며 웹앱에서 변경 가능합니다.
- 기준가격과 현재 AdminPlus 상품가격이 달라지면 상단 경고와 `API 상품매칭` 화면에 가격변동 알림을 표시합니다.
- 같은 상품·같은 변경가격에 대한 미확인 알림은 중복 생성하지 않습니다.
- 사용자가 가격변동을 확인한 뒤 `현재가를 기준으로`를 누르면 현재가를 새 기준가격으로 저장하고 알림을 확인 처리합니다.
- AdminPlus 계정 상태에 `order.read`, `product.read` 확인을 표시합니다.

## 기존 기능 유지
- 쿠팡 쿠폰 자동운영/안전교체/옵션ID 중복판정
- 쿠팡·토스 주문수집 및 4단계 주문현황
- 기존 수동/엑셀 업체 발주
- AdminPlus 설정시간별 주문등록 및 송장회수·쿠팡/토스 송장등록
- AdminPlus/Toss 토큰 자동갱신 및 웹앱 인증키 관리

## 주의
- AdminPlus 공식 Seller API의 상품 목록 응답은 상품 단위 `price`와 옵션의 코드/명칭/재고를 제공합니다. V209 공급가 감시는 상품 `price` 기준입니다.
- AdminPlus 상품조회에는 `product.read`, 매칭조회에는 `order.read`, 매칭등록에는 `order.write` 권한이 필요합니다.
- 주문 등록과 결제 접수는 별도 단계입니다. V209는 안전을 위해 AdminPlus 주문 등록까지만 자동화하며 결제 접수는 실행하지 않습니다.
